# คัดลอกไฟล์นี้เป็นชื่อ ".env" แล้วแก้ค่าให้ตรงกับเครื่องของคุณ
# รูปแบบ: postgresql://ชื่อผู้ใช้:รหัสผ่าน@localhost:5432/ชื่อฐานข้อมูล
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/alicornspeak

# พอร์ตที่ backend จะรัน (ค่าเริ่มต้นตรงกับที่ frontend เรียกอยู่แล้ว)
PORT=3000
